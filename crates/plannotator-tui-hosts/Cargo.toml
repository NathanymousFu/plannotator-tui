[package]
name = "plannotator-tui-hosts"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
keywords = ["annotation", "markdown", "agents"]
categories = ["text-processing"]
description = "Find a coding agent's transcript and read its recent messages (Claude Code, Codex, pi)."

[dependencies]
rusqlite = { version = "0.31.0", features = ["bundled"] }
# dsh appends one zstd frame per flush; a Rust decoder keeps the reader in-process.
ruzstd = "0.9.0"
serde_json.workspace = true

[lints]
workspace = true
